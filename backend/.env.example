# Servidor
PORT=3000
The .env.example file is added to document the PORT environment variable, but the application has no mechanism to load .env files at runtime. The dotenv package is not installed, and there's no code importing or requiring it. As a result, developers creating a .env file based on the example will find their configuration is silently ignored, and the application always uses the hardcoded fallback value.